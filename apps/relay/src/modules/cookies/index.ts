import type { FastifyInstance } from "fastify";

// Cookies module (T7 fills this in).
// Owns: first-party visitor cookie handling (issue, refresh, read) and
// journey_id cookie continuity.
export async function registerRoutes(_app: FastifyInstance): Promise<void> {
  // no-op until T7
}
